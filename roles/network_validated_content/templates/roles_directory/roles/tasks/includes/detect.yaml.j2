---
- name: Invoke detect function
  ansible.builtin.include_role:
    name: network.base.resource_manager
  vars:
    action: 'detect'
    state: 'replaced'
    resources:
      {% for r in resources %}
      - "{{r}}"       
      {% endfor %} 